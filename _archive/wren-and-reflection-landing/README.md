# Wren & Reflection — archived landing shell

Saved when the site switched to serving the flipbook at `/wren-and-reflection/`.

| File | Original location |
|------|-------------------|
| `page.md` | `_pages/wren-and-reflection.md` |
| `layout.html` | `_layouts/wren-and-reflection.html` |
| `style.css` | `assets/css/wren-and-reflection.css` |

This folder is not published (Jekyll ignores `_`-prefixed dirs unless listed in `include`).

To restore the landing page later, copy these files back to their original paths and remove or rename `wren-and-reflection/index.html`.
